version: '3'

services:

  fogbus2-naive_formula3:
    # image: fogbus2-naive_formula3
    image: cloudslab/fogbus2-diabetes_prediction
    build:
      context: ../../
      dockerfile: dockerFiles/DiabetesPrediction/Dockerfile
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Australia/Melbourne
    network_mode:
      host
