version: '3'

services:

  game_of_life30:
    container_name: game_of_life30
    image: game_of_life30
    build:
      context: ../../
      dockerfile: ./tasks/GameOfLife30/Dockerfile
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Australia/Melbourne
    network_mode: "host"

